# Copy to keystore.properties (same folder) and fill in. Never commit keystore.properties.
# Put your .jks / .keystore file in android/app/ (see storeFile path below).

storePassword=your-store-password
keyPassword=your-key-password
keyAlias=your-key-alias
storeFile=release.keystore
