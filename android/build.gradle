buildscript {
    ext {
        buildToolsVersion = "36.0.0"
        minSdkVersion = 24
        // compileSdk 36: the Android 17 Live Update APIs (MetricStyle, Semantic
        // Color, direct setRequestPromotedOngoing) are all invoked through
        // REFLECTION in MihrabLiveActivityModule.kt, so they degrade gracefully
        // and compile fine against 36. The repo dropped plain "platforms;android-37"
        // (only "android-37.0" exists now), which AGP 8.12 cannot consume.
        compileSdkVersion = 36
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"

