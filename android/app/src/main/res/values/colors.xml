<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Adaptive icon backdrop — matches the deep-teal of the Mihrab logo
         so the adaptive mask reads as a single piece on launchers that
         crop the foreground tighter than the foreground's own background. -->
    <color name="ic_launcher_background">#0F4D44</color>
</resources>
