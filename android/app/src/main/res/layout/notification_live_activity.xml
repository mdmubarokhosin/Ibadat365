<?xml version="1.0" encoding="utf-8"?>
<!--
  Custom collapsed content view for the Mihrab Live Activity notification.
  DecoratedCustomViewStyle wraps this in the standard notification chrome
  (app icon + app name header row), so this view is only the content area.

  Row 1: prayer title (left, expands) · countdown | percentage (right, fixed)
  Row 2: horizontal progress bar (full width)

  Uses @android:style/TextAppearance.Material.Notification.* so text colour
  adapts to light / dark notification backgrounds on every shell — no
  hardcoded colours that break in one theme or the other.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Row 1 ─────────────────────────────────────────────────────── -->
    <!-- layoutDirection=ltr keeps prayer-title LEFT and countdown RIGHT
         even when the title text is RTL (Arabic/Urdu). Individual text
         direction is still resolved from locale so Arabic glyphs render
         correctly — we are only fixing the widget *order*, not the text. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layoutDirection="ltr"
        android:gravity="center_vertical">

        <!-- Left: "الفجر · 02:48" — expands, truncates with ellipsis -->
        <TextView
            android:id="@+id/la_prayer_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textDirection="locale"
            android:textAppearance="@android:style/TextAppearance.Material.Notification.Title" />

        <!-- Right: "↓ 1h 23m  |  52%" — always pinned to the far right -->
        <TextView
            android:id="@+id/la_countdown_pct"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textDirection="ltr"
            android:textAppearance="@android:style/TextAppearance.Material.Notification" />

    </LinearLayout>

    <!-- Row 2: progress bar ────────────────────────────────────────── -->
    <ProgressBar
        android:id="@+id/la_progress"
        style="@android:style/Widget.Material.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:max="100"
        android:progress="0" />

</LinearLayout>
