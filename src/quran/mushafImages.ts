/**
 * The one number the page-image muṣḥaf left behind.
 *
 * This module was the URL space and crop table for the 604 page PNGs on
 * the `mushaf-assets-v2` release. The font-rendered reader replaced the
 * images in 2.8.0 and the image reader is gone now, but the page count is
 * the muṣḥaf's, not the images' — the standard Madinah print — and half
 * the Quran module reads it from here.
 */

/** Total number of pages in the muṣḥaf — matches the standard Madinah print. */
export const MUSHAF_TOTAL_PAGES = 604;
