/** Settings → Quran. Reading mode, the companion text, downloads. */
import { QuranCard } from '../QuranCard';
import { SettingsPage } from '../SettingsPage';

export function QuranSettingsScreen() {
  return (
    <SettingsPage>
      <QuranCard />
    </SettingsPage>
  );
}
